{
  "name": "sparkpack-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/app",
    "build": "npm run build --workspace=packages/visuals && npm run build --workspace=packages/app",
    "storybook": "npm run storybook --workspace=packages/visuals",
    "typecheck": "npm run typecheck --workspace=packages/visuals && npm run typecheck --workspace=packages/app",
    "derive-questions": "node scripts/derive-ks1-2023-questions.mjs"
  }
}
